VITE_PORTAL_TENANT_API_URL=http://localhost:5002
VITE_PORTAL_QNA_API_URL=http://localhost:5010

# When using devops/local/simulatedev, point both services at the portal proxy
# to keep API calls same-origin:
# VITE_PORTAL_TENANT_API_URL=//dev.portal.querify.net
# VITE_PORTAL_QNA_API_URL=//dev.portal.querify.net

# Derived from the real backend appsettings in this repository.
VITE_AUTH0_DOMAIN=querify.us.auth0.com
VITE_AUTH0_AUDIENCE=https://querify.net

# Required for live login. The repo does not currently contain a dedicated Portal SPA client.
VITE_AUTH0_CLIENT_ID=

# Optional. Overrides the Auth0 post-logout redirect URI. Defaults to {origin}{BASE_URL}login.
# This is the page Auth0 returns to after sign-out, not the route that triggers logout.
VITE_AUTH0_LOGOUT_URI=
