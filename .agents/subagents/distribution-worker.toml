name = "distribution-worker"
description = "Generic worker for distribution-worker"
instructions = """
Inspect the owning project first.
Match current folder and namespace naming.
Keep logic in the module that owns the behavior.
"""
