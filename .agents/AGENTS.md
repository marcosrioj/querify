# Agents

Use the current project pattern.

- Match the owning project, folder, namespace, and file naming before editing.
- Keep state changes in command handlers.
- Keep read shaping in query handlers.
- Do not add extra layers when a direct handler implementation is enough.
- Keep files real and owned by the project that compiles them.
- Prefer simple integration coverage when behavior depends on persistence.
